You are tasked with reducing experimental protocols down to a few parameters, represented in JSON. Do this for every control and experiment.
Ensure that all relevant controls for every experimental variable are included, such as a negative control. If any necessary information is missing, use your knowledge to analyze the prompt and fill in the gaps.
Always include units. Each treatment and supplementation combination should be listed as its own experiment. Every condition or dose change counts as a new combination. It is crucial that you remain consistent. Distinguish carefully between what is considered a supplement and what is considered a treatment.
Separate each experiment with a comma. At the top of your response, specify which logic/prolog program was selected before defining the JSON outputs.
Here is the template, and do not include any escape tokens in the output:

{
  "logic_program": <String>,
  "experiments": [
    {
  "experiment": <INT>,
  "baseline_media": <String>,
  "media_supplementation": <String>,
  "media_supplementation_doses": <String>,
  "treatment": <String>,
  "treatment_parameters": <String>,
  "sampling_time": <Float>,
  "type": <String>,
  "summary": <String>
}, ... ]}

If a category is not applicable, set it to null. "type" can be one of "control" (no supplementation or treatment), "supplementation control" (only supplement), "treatment control" (only treatment), "experiment" (both) or "negative control" (everything involving the negative control supplement). All of these should be present.


